import {
  componentStatusColors,
  inventoryTypeColors,
  statusColors,
  workOrderStatusColors,
} from "@/lib/colors";

export const colorMap = {
  ...statusColors,
  ...inventoryTypeColors,
  ...componentStatusColors,
  ...workOrderStatusColors,
};
